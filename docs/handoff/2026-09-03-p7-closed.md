# Handoff -- P7 closed: everything landed on main

2026-09-03. Newest commit this brief describes: `014992e` (merge of PR #3,
the post-merge docs). Pick-up measures drift from here. This entry exists so
the next session starts from a fully-landed anchor; the build's own record is
`2026-09-03-p7-grpc-read-api-built.md`, and `2026-09-03-p7-merged.md` records
the merge. Nothing in this entry is new evidence; it closes the loop the
previous entry left open.

## Current state

- **built** -- Seven properties CLAIMABLE on `main`, including P7 (read-only
  gRPC API, wire fidelity). All three PRs merged: #1 Lane 1, #2 P7, #3 docs.
  re-verify: `git log --oneline -1 origin/main` -> `014992e Merge pull request #3 ...`
  re-verify: `sh gates/run.sh` -> last line `ok lane1 claimable=7/7`, 18 rows in `gates/out/`
  re-verify: `git merge-base --is-ancestor 9898cba origin/main && echo yes` -> `yes`
- **built** -- CI green on `main` for the P7 merge (run 33816126081,
  `ubuntu-24.04`, `ok proto fresh`, `ok lane1 claimable=7/7`).
  re-verify: `gh run list --branch main --limit 1 --json conclusion --jq '.[0].conclusion'` -> `success`
- **built** -- Ledgers: six 2026-09-03 learnings entries, three 2026-09-03
  handoff entries, build ledger `docs/2026-09-03-p7-build-ledger.md`.
  re-verify: `ls docs/learnings | grep -c 2026-09-03` -> 6
- **not started** -- BASELINE registration of the seven cells; Lanes 2-3;
  the three pre-existing unfalsified checks; the two P7 legs (`records`,
  `compared`) never driven non-zero; D2; generator RNG via getrandbits.

## Locked decisions

Unchanged from `2026-09-03-p7-grpc-read-api-built.md` (six decisions) -- all
reasons still hold at `014992e`. One process fact worth locking: **a docs
commit authored after a push may miss the merge** (the operator merged PR #2
within minutes of the push; `9898cba` needed PR #3). Reason: the operator
merges fast; say "one more commit coming" before pushing, or push docs first.

## Reuse map

As in the build entry. Entry points for the next session: `internal/reader`
(the Lane 2 seam), `readgrpc.InProcess` (portless gRPC client for gates),
`gates/p7_test.go` (behavioural twins), `gates/run.sh` `== proto fresh`
(enumerate-both-ways freshness gate).

## Invariants

As in the build entry. The one most likely to be violated next: **a check
whose every twin plants 0 is unfalsified** -- Emit refuses a missing key, not
a dead increment path. Any new gate must give every check a nonzero
expectation in at least one twin.

## Open / next

1. BASELINE registration of the seven `GATE_VERDICT` cells. The seam is
   undesigned; BASELINE hashes LF-normalised bytes; MERIDIAN's rows already
   use the 16/17-key schema. Blocker: none, but it is a design question
   before a build (brainstorm, then plan).
2. Optional third P7 twin driving the `records` and `compared` legs non-zero
   (a feed one record longer, served as base). Cheap; not promised.
