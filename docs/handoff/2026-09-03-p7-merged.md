# Handoff -- P7 merged to main, CI green on Linux

2026-09-03. Newest commit this brief describes: `ecdd82d` (merge of PR #2,
`p7-grpc-read-api` into `main`, merged 2026-09-03T23:06:22Z). Pick-up measures
drift from here. Read `2026-09-03-p7-grpc-read-api-built.md` first for the
build's own state of record; this entry records only what changed after it.

## Current state

- **built** -- P7 is on `main`. Seven properties CLAIMABLE.
  re-verify: `git log --oneline -1 main` -> `ecdd82d Merge pull request #2 ...`
  re-verify: `sh gates/run.sh` -> last line `ok lane1 claimable=7/7`
- **built** -- CI on the merge commit: run 33816126081, `ubuntu-24.04`, printed
  `ok proto fresh` and `ok lane1 claimable=7/7` with the P7 row. Earlier runs
  33815809956 / 33815870881 on `4d2b21d` printed the same.
  re-verify: `gh run view 33816126081 --log | grep -E "ok proto fresh|ok lane1 claimable"`
- **in-progress** -- one docs commit missed the merge: `9898cba` (`docs: CI
  measures buf codegen and P7 on Linux`: STATUS sub-limit (6) narrowed with
  the run ids; learnings entry `2026-09-03-ci-measures-buf-codegen-and-p7-on-linux.md`)
  is on `origin/p7-grpc-read-api` but not in `main`. It lands with this entry.
  re-verify: `git merge-base --is-ancestor 9898cba origin/main && echo yes || echo no`

## Locked decisions

Unchanged from the build entry. One reason narrowed: the build entry listed
CI as "not verified"; it is now measured on three runs, so the honest limit
about Windows-only codegen is retired in STATUS.md (`9898cba`).

## Open / next

1. Land `9898cba` + this entry via a second PR (commands in the operator's
   hands; the build entry's commit sequence is spent).
2. BASELINE registration of the seven cells (seam undesigned; LF-normalised
   hashes). 3. The three pre-existing unfalsified checks and the two P7 legs
   never driven non-zero. 4. Lanes 2-3 remain promised by no one.
