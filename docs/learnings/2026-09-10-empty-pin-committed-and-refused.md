ts: 2026-09-10T04:47:14Z
commit: cdee383
session: claude-code meridian-datum-adoption (https://claude.ai/code/session_01X6SuqCyyde9KbvvmANfCnK)
status: verified
fact: The pin binding the vendored conformance pack was committed and pushed EMPTY (0 bytes, blob e69de29, added in `b3d19ce`), and CI on `main` went red at `--verify-pin` -- this repo's first red CI. The cause is the shape of the write step, not the pack: `check.mjs --write-pin <sha>` streams the pin to stdout for a shell redirect, so the redirect creates and truncates `gates/datum/PIN` before node runs, and any failure of that node invocation leaves a 0-byte file that looks exactly like a written pin. Nothing between writing and pushing reads the pin, so emptiness surfaced only in CI. What the empty pin did NOT do is credit anything: the same run printed the pack's own self-test green (`13 positive, 46 negative, 17 reasons, 13 rules mutated`) and `ok lane1 claimable=7/7`, and the claim was still refused, because the rows were checked by a pack whose bytes nobody had verified. Fail-closed at the pin is the whole difference between "a pack said yes" and "the pinned pack said yes". Writing a pin by shell redirect needs a non-empty guard (write to a temp file, check the first line matches `datum <40-hex>` and the line count equals the vendored file count plus one, then move it into place); the durable fix is for `--write-pin` to write the file itself, proposed in the governing text's repo, not taken here because it would move the pin this repo just fixed.
basis: `git cat-file -s $(git rev-parse cdee383:gates/datum/PIN)` -> `0`. CI run 34410348602 on `cdee383`, conclusion `failure`, its log showing `== conformance pack self-test` then `ok conformance: 13 positive, 46 negative, 0 real, 17 reasons, 13 rules mutated`, then `== claimability` / `ok lane1 claimable=7/7`, then `== conformance pack over the rows` / ``PIN first line must be `datum <sha>` `` followed by `PIN mismatch: ... present but unlisted`. Reproduced locally against an emptied copy: 65 stderr lines, one ``PIN first line must be `datum <sha>` `` plus 64 `present but unlisted` (one per vendored file), exit 2. Fixed in the working tree at write time (uncommitted): pin written to a temp file under a first-line and 65-line guard, then `sh gates/run.sh` ends `ok conformance pack agrees: claimable=7`, exit 0.
re-verify: d=$(mktemp -d) && cp -r gates/datum/. "$d" && : > "$d/PIN" && node "$d/check.mjs" gates/out --verify-pin 2>&1 | head -1
