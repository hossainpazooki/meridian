ts: 2026-09-06T16:59:42Z
commit: a087ab2
session: claude-code dev workspace, baseline pick-up + DATUM brainstorm (6f109325-2283-4a8f-b469-7dc475706f44); https://claude.ai/code/session_01KD8VJDoLENhTTtiXvPE72K
status: refuted-assumption
fact: "Registration into BASELINE is a copy, not a translation" (design §4, STATUS.md crediting rule) holds at the key-set level and fails at the checker. All 18 rows from the 2026-09-06T16:53Z run, renamed to BASELINE's `<surface>-lane<lane>-<cell>-<stamp>.json` filename rule and hash-bound in a scratch SOURCE.md, are refused by BASELINE's `check-ledger.mjs` at baseline `775978f` on two rules: every row fails `rows must equal evaluated.no_future_accepted` (a PARALLAX check name bound into the generic checker; MERIDIAN has no such check), and P4, P6, P7 fail `second twin cell for <surface>:lane1` because `groupCells` holds at most one twin per surface and lane. Everything else passed: the 16/17-key schema, enums, plant match over the union of keys, the evaluated-zero rule, LF-normalized hash binding. BASELINE's `build.mjs` additionally refuses any ledger with more than one gate surface. The same day the operator ruled that BASELINE is not a catalog and that DATUM (`~/dev/datum`, `91be859`) governs meridian instead, so the registration item in `2026-09-03-p7-closed.md` is retired rather than fixed.
basis: `node probe.mjs <scratch>` building three scratch ledgers and running `node ~/dev/baseline/scripts/check-ledger.mjs <dir>` on each -> `===== all18: exit 1` followed by 18 lines `FAIL verdicts/meridian-lane1-p*-lane1-*.json: rows must equal evaluated.no_future_accepted` and one line `FAIL verdicts/meridian-lane1-p4-lane1-twin-20260906T165328.354549Z.json: second twin cell for meridian-lane1-p4:lane1 (have verdicts/meridian-lane1-p4-lane1-twin-20260906T165328.321172Z.json)`; `===== p1only: exit 1` with the two `rows must equal` lines only. Rows read from `gates/out/` as written by the 16:53Z `run.sh` at a087ab2; probe artifacts in the writing session's scratchpad, not committed.
re-verify: grep -n 'evaluated.no_future_accepted\|second ${row.cell} cell' ~/dev/baseline/scripts/lib/ledger.mjs
