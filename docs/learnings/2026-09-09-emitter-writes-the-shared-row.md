ts: 2026-09-09T22:10:00Z
commit: uncommitted (emitter change not yet committed at time of writing; HEAD 3c454ed)
session: claude-code meridian-datum-adoption
status: verified
kills: 2026-09-01-baseline-gate-verdict-schema.md (the key names: `parallax_sha`/`parallax_worktree` are gone and `schema` is added; the 16/17-key count and the rest stand)
kills: 2026-09-01-baseline-twin-rows-carry-planted.md (the "16 keys" arithmetic only: live rows now carry 17 keys and twin rows 18; `planted` and its three sub-keys stand)
fact: `Emit` in `gates/verdict.go` writes the shared verdict row of the governing text's conformance pack, vendored under `gates/datum/`: a `schema` key (`datum/gate-verdict/1`), and the emitter's commit and tree state under the emitter-neutral keys `gate_sha` / `gate_worktree` instead of keys named for another project. Before the change the vendored checker refused all 18 rows with exactly five reasons per row (missing `schema`, `gate_sha`, `gate_worktree`; unknown `parallax_sha`, `parallax_worktree`) and nothing else; after it, the same checker accepts all 18 and derives seven surfaces CLAIMABLE with twin counts 1/1/1/2/1/3/2, the same table `gates/claimability.py` prints. The first checker run over real rows was red before the emitter moved, so the green is the emitter change and not a checker that was never pointed at the rows. `claimability.py` reads none of the renamed keys (grep: it keys on `surface`, `cell`, `result`, `checks`, `planted`), so it did not change for the rename; it gained `--render` / `--check` for the generated STATUS.md block.
basis: before: `node gates/datum/check.mjs gates/out | sed -E 's/^FAIL [^:]+: //' | sort | uniq -c` -> five lines, each `18`, named above, exit 1. after (`sh gates/run.sh` through claimability, `ok lane1 claimable=7/7`): `node gates/datum/check.mjs gates/out` -> seven `CLAIMABLE` lines then `ok 18 rows conform, 7 surfaces`, exit 0; a fresh P7 live row's sorted keys are `cell checks content_hash content_hash_basis evaluated gate_sha gate_worktree kind lane params ran_at result rows runner schema scope surface` (17). `go test ./gates -run TestEmit` was red on the key list before the emitter change and green after.
re-verify: sh gates/run.sh >/dev/null 2>&1; node gates/datum/check.mjs gates/out | tail -1
